# Handoff — 2026-05-11

## Shipped today (commits 1faa2fc, 7f4e466)

**Feature: user label override + movability hooks for resident-robot.**
PR-sized vertical slice. `PATCH /objects/{oid}` lets a user pin a label
(`label_user`) or declare a movability class on any WM object. The pin
beats autonomic CLIP labels everywhere it matters: API responses,
ingest payloads, FAISS dedup keys.

Files touched (8):
- `rtsm/api/server.py` — PATCH endpoint, _obj_summary surfaces new fields
- `rtsm/stores/working_memory.py` — ObjectState fields, update_user_fields(),
  promote_min_conf lifted to config (0.05→0.18 default)
- `rtsm/stores/vectors/faiss_client.py` — dedup keyed on display_label
- `rtsm/cfg/clip/vocab.yaml` — CLIP gates restored toward pre-demo strictness
- `rtsm/cfg/rtsm.yaml` — declares object.promote_min_conf
- `tests/test_object_patch.py` — 27 assertions, dual-mode (script + pytest)
- `docs/design/persistence.md` — six-class movability taxonomy
- `docs/design/demo-config-audit.md` — justification for the retunes

**Milestone: first end-to-end apartment-data replay.**
193 confirmed WM objects, 38 unique labels, 500 frames, zero errors.
See `docs/2026-05-11-apartment-replay.md`.

## Test status

- `tests/test_object_patch.py` — 27/27 passing in script mode
  (`docker exec rtsm-dev python3 /workspace/rtsm/tests/test_object_patch.py`)
- Pytest mode: untested. Pytest is **not** a declared project dependency
  (no mention in `pyproject.toml` / requirements). `tests/test_serve_api.py`
  uses pytest fixtures, so the convention exists informally. The test file
  is structured for both modes; whoever wires up pytest formally needs to
  verify import-time-assertion → pytest-AssertionError flow works.

## Known dangling threads

1. **Pytest as dev dependency.** Not declared. Either add it to
   `pyproject.toml` `[project.optional-dependencies] dev = [...]` or
   make a deliberate decision to keep tests script-mode-only. The
   current state is "implicit pytest" which is the worst of both.

2. **`docs/design/structure-layer.md`.** Referenced as TODO in
   `rtsm/cfg/clip/vocab.yaml:30`. The structure-detection layer is
   vestigial scaffolding — typed slot + planarity computation but no
   consumer. When this is picked up, the doc should cover: why CLIP
   can't classify structural surfaces, what the planarity-based router
   should look like, migration plan for existing FAISS records like
   `floor_001`.

3. **Movability-driven eviction logic.** The taxonomy exists (six-class
   ladder in `persistence.md`) and ObjectState carries `movability_class`,
   but no code consumes it yet for eviction decisions. Currently it's
   pure metadata. Next step: `LongTermMemory` evict policy reads
   `movability_class` and weights TTL accordingly.

4. **PATCH endpoint UX.** No web UI surface for it yet. CLI-only via
   `curl`/`httpx`. If there's a frontend planned, it needs an "edit
   label" affordance on the object detail view.

## What I'd pick up next session

In priority order:

1. **Push to origin** if you haven't yet: `git push origin main`
2. **Decide on pytest.** 30-min decision + small commit either way.
3. **Run the apartment replay again** to confirm the retuned CLIP gates
   (`min_top 0.22`, `min_margin 0.03`, `promote_min_conf 0.18`) don't
   regress the 193-confirmed-objects baseline. Compare object counts
   and label distributions against today's milestone numbers.
4. **Wire movability into LTM eviction.** This is the actual payoff of
   the taxonomy work — until eviction reads it, the field is decorative.

## Process notes from today

- Caught a path bug (`~/rtsm/cfg/...` vs `~/rtsm/rtsm/cfg/...`) only
  after a Python crash. The repo-name / package-name collision is a
  trap — prefer cwd-relative paths or verify with `p.exists()` upfront.
- Surveyed full `git diff --stat` before staging. Caught that scope was
  much bigger (5 modified + 3 new) than the working assumption (just
  config + test). Worth the 30 seconds every time.
- Caught one dangling doc reference (`structure-layer.md`) via a
  pre-commit grep. Rephrased to honest TODO instead of removing or
  redirecting. Lesson: grep for `docs/` references before committing
  anything that adds them.
