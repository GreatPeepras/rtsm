# Claude Code
.claude/
CLAUDE.md

# Debug outputs (generated by scripts/debug_segmentation.py)
debug/

# Video source files (keep GIFs, not raw MP4s)
repo_media/*.mp4

# Evaluation reports (generated by benchmark/eval scripts — regenerate with current config)
reports/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so
*.pyd
*.dll

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
pytestdebug.log

# Type checkers
.mypy_cache/
.pyre/
.pytype/
.ruff_cache/

# Jupyter Notebook
.ipynb_checkpoints

# Environments
.env
.env.*
*.env
venv/
.venv/
ENV/
env/
.venv.bak/

# IDE/editor
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# UV build cache
__pypackages__/

# Docker
/docker/.env
/docker/*.env

# Logs
*.log

# Local data/model artifacts (as requested)
models/clip/**
!models/clip/.keep

# Datasets (ignored for now)

test_dataset/
*.ply

# Model store
model_store/
# model weights
*.pt
*.pth
*.onnx
*.ckpt
mobileclip2_b.ts

# Demo frontend
demo/node_modules/
demo/dist/

# Config symlink (canonical files live in rtsm/cfg/, config/ is a symlink)
config

# MkDocs build output (should not be committed; deploy via CI or a gh-pages branch)
site/
# Recordings: ignore everything except curated demo data that ships with the repo.
# User recordings should live in /mnt/rtsm-data/rtsm-recordings/ (bind-mounted from host),
# but this is a safety net in case someone uses a relative path like `--record recordings/foo`.
/recordings/*
!/recordings/session1/
!/recordings/demo_clip/
*.PRE_*

# Local backup files from in-place patches
*.bak.*
.weekend-snapshot/

# Per-run audit logs from merge/auto-merge tooling — kept locally, not versioned
scripts/merge_review_log_*.tsv
scripts/auto_merge_log_*.tsv
auto_merge_log_*.tsv
merge_review_log_*.tsv


# Stray duplicates of source-of-truth files (canonical lives elsewhere)
/scripts/working_memory.py
/scripts/working_memory_old.py

# Stale .bak archives (moved to bak/ locally; not versioned)
*.bak

# Stray empty/typo artifact at repo root
/.sh
/bak/
/bak/

# deploy-script pre-patch backups (never source of truth)
*.pre-*
scripts/rtsm_decisions*.json
scripts/rtsm_cleanup_log_*.tsv
scripts/rtsm_cleanup_remap_*.json
scripts/review.html
